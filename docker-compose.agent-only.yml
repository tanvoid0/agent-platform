# Alias for the default (backend-only) stack, kept for scripts and docs.
#   docker compose -f docker-compose.agent-only.yml up --build

services:
  server:
    extends:
      file: docker-compose.yml
      service: server

volumes:
  agent_platform_data:
