# Same as docker-compose.yml — explicit API-only alias for scripts and docs.
#   docker compose -f docker-compose.agent-only.yml up --build

services:
  agent-platform:
    extends:
      file: docker-compose.yml
      service: agent-platform

volumes:
  agent_platform_data:
