# Backend-only override for scripts and docs.
#   docker compose -f docker-compose.agent-only.yml up --build

services:
  server:
    extends:
      file: docker-compose.yml
      service: server
  client:
    profiles: ["__off__"]

volumes:
  agent_platform_data:
