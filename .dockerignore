# Build context is the agentic-ai repo root (see docker-compose.yml).
# Root .dockerignore applies; this file documents local exclusions.
app/agent_runs.db
app/agent_runs.db-shm
app/agent_runs.db-wal
