# Archived Flow UI (agent-platform web)

This is a snapshot of gent-platform/web before the Delegation UI migration.

- Not imported by the build (live app is ../web).
- Reference for TanStack Query keys, hooks, and API client during migration.
